version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=\${DATABASE_URL}
      - SUPABASE_JWT_SECRET=\${SUPABASE_JWT_SECRET}
    restart: always
